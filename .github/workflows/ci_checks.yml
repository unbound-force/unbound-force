# SPDX-License-Identifier: Apache-2.0
#
# CI Checks
# =========
# Runs MegaLinter (Go, Actions, Bash, Gitleaks) and
# validates PR titles against conventional commit format.
# Consumes the reusable CI workflow from org-infra.

name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read
  issues: none
  pull-requests: none

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  call_reusable_ci:
    name: Standardized CI
    uses: complytime/org-infra/.github/workflows/reusable_ci.yml@2c193416adbc621a64021ace52bf57114d703888 # v0.3.1
    permissions:
      contents: read
      issues: read
