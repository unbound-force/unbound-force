## Why

The `/finale` command's PR description generation (Step 5c)
produces a minimal `## Summary` with bullet points from commit
history. This is underspecified compared to sibling commands
(`/unleash` Step 8, `/review-pr` attribution). The current
state has several gaps:

1. **No structured sections** — reviewers lack "how to test"
   and "how to demo" guidance, forcing them to reverse-engineer
   verification steps from commit diffs.
2. **No AI attribution** — PR descriptions and commit messages
   generated by `/finale` lack provenance indicators, making
   it impossible to distinguish AI-generated content from
   human-written content.
3. **No review-council findings** — unresolved findings from
   `/review-council` are lost when the PR is created, leaving
   reviewers without context on known issues.
4. **No PR template detection** — `/finale` ignores repository
   PR templates (`.github/PULL_REQUEST_TEMPLATE.md`), producing
   inconsistent PR formats across repos that use templates.
5. **Shell injection vector** — the current `gh pr create`
   call uses inline `--title` and `--body` shell interpolation,
   which is unsafe when AI-generated content contains shell
   metacharacters.

Addresses: #142

## What Changes

Enhance `/finale` Step 5 (PR creation) and Step 3 (commit
message generation) to produce richer, safer, and more
transparent output.

## Capabilities

### New Capabilities
- `structured-pr-body`: Generate PR body with structured
  sections (What Was Done, How to Test, How to Demo, Key
  Files Changed) mirroring `/unleash` Step 8 patterns
- `pr-attribution`: Add AI attribution footer to PR
  descriptions: `_This PR was generated by /finale
  (AI-assisted)._`
- `commit-attribution`: Add AI attribution to commit
  messages via both a git trailer (`AI-assisted-by:
  /finale`) and a human-readable footer line
- `review-council-findings`: Surface unresolved
  `/review-council` findings in a "Known Issues" section
  of the PR body
- `pr-template-detection`: Detect and respect
  `.github/PULL_REQUEST_TEMPLATE.md` when present,
  mapping generated content to template sections

### Modified Capabilities
- `pr-creation`: Switch `gh pr create --body` to
  `--body-file` pattern to eliminate shell injection
  vector (consistent with `/review-pr`'s safe pattern)
- `commit-message-generation`: Extend Step 3 to append
  AI attribution trailer and footer to generated commit
  messages

### Removed Capabilities
- None

## Impact

- **Files changed**: `.opencode/commands/finale.md` and
  its scaffold copy at
  `internal/scaffold/assets/opencode/commands/finale.md`
- **Drift detection**: `TestEmbeddedAssets_MatchSource`
  enforces byte-identity between the two copies
- **Cross-repo propagation**: Changes propagate to all
  hero repos via `uf init` scaffold sync
- **Existing guardrails preserved**: User confirmation
  gates for commit messages and PR content remain in
  place — the new structured content flows through the
  same approval step
- **No Go code changes**: This is a Markdown-only change
  to slash command instructions

## Constitution Alignment

Assessed against the Unbound Force org constitution
v1.2.0.

### I. Autonomous Collaboration

**Assessment**: PASS

Structured PR descriptions with provenance metadata
make `/finale` output self-describing. Reviewers can
interpret the PR without consulting the producing agent.
The review-council findings section enables artifact-
based communication between `/review-council` and
`/finale` without runtime coupling.

### II. Composability First

**Assessment**: PASS

All new capabilities degrade gracefully. PR template
detection is optional (falls back to default format).
Review-council findings are included only when available.
Attribution is always added. No new mandatory
dependencies are introduced.

### III. Observable Quality

**Assessment**: PASS

AI attribution in PR descriptions and commit messages
adds provenance metadata, directly satisfying "which
hero produced the output." The structured sections
produce consistent, parseable PR content. The shell
injection fix eliminates an integrity risk in output
generation.

### IV. Testability

**Assessment**: PASS

The change is Markdown-only (slash command instructions).
Existing drift detection tests
(`TestEmbeddedAssets_MatchSource`) verify scaffold sync.
No new Go code is introduced. The structured sections
are human-verifiable during the user confirmation step.

### V. Security by Default

**Assessment**: PASS

The shell injection fix (switching from inline `--body`
to `--body-file`) directly addresses input validation
for AI-generated content reaching shell execution. This
aligns with the principle's requirement that "all
external inputs MUST be validated and sanitized before
reaching any security-sensitive operation (shell
execution)."
<!-- scaffolded by uf vdev -->
