<!--
  [P] marks tasks eligible for parallel execution.
  Add [P] when a task: (a) touches different files from
  other [P] tasks in the group, (b) has no dependency
  on prior tasks in the group, (c) can safely execute
  without ordering constraints.
  Do NOT add [P] when tasks modify the same file —
  parallel workers will cause merge conflicts.

  NOTE: All tasks in groups 1-5 modify the same file
  (.opencode/commands/finale.md) so none are parallel-
  eligible. Group 6 (scaffold sync) depends on all
  prior groups completing.
-->

## 1. Shell Injection Fix (Prerequisite)

- [x] 1.1 In `.opencode/commands/finale.md` Step 5e,
  replace inline `--body "<body>"` with `--body-file`
  pattern: write approved PR body to a temp file
  (`mktemp`), `chmod 600`, pass via
  `gh pr create --body-file <file>`, then `rm -f` in
  all exit paths (success, failure, abort). Keep
  `--title` as inline since it is single-line and
  user-approved. Update both the fork and non-fork
  `gh pr create` invocations (lines 186-189).

## 2. Structured PR Body Sections

- [x] 2.1 In `.opencode/commands/finale.md` Step 5c,
  replace the current "summarize all commits with a
  `## Summary` section and bullet points" instruction
  with structured section generation:
  `## Summary` (what was done, from commit analysis),
  `## How to Test` (verification steps — pull from
  spec acceptance scenarios if on an `opsx/*` or
  `NNN-*` branch, otherwise synthesize from diff),
  `## How to Demo` (demo walkthrough for reviewers),
  `## Key Files Changed` (file listing with brief
  descriptions from `git diff --stat`). Each section
  MUST contain substantive content, not placeholders.

## 3. PR Template Detection

- [x] 3.1 In `.opencode/commands/finale.md`, add a new
  sub-step before Step 5c: check for
  `.github/PULL_REQUEST_TEMPLATE.md` (case-insensitive,
  also check `.github/pull_request_template.md`). If
  found, read the template and use its `##` heading
  structure as the skeleton for the PR body, mapping
  generated content to matching sections. Unmatched
  template sections are preserved as-is for the user
  to fill in during the approval step. If no template
  is found, fall back to the default structured format
  from task 2.1.

## 4. Review-Council Findings

- [x] 4.1 In `.opencode/commands/finale.md`, add
  instructions after structured body generation
  (Step 5c) to check the conversation context for
  prior `/review-council` output. If unresolved
  findings exist (findings acknowledged but not fixed),
  add a `## Known Issues` section listing each finding
  with severity and brief description. The section
  appears between `## Key Files Changed` and the
  attribution footer. If no review-council was run or
  no unresolved findings exist, omit the section.

## 5. AI Attribution

- [x] 5.1 In `.opencode/commands/finale.md` Step 5c,
  append the attribution footer to every generated PR
  body as the last line:
  `_This PR was generated by /finale (AI-assisted)._`
  The footer MUST be included in the proposed content
  shown to the user for approval (Step 5d).
- [x] 5.2 In `.opencode/commands/finale.md` Step 3b,
  add instructions to append AI attribution to
  generated commit messages: (1) a git trailer
  `AI-assisted-by: /finale` and (2) a human-readable
  footer `Generated with AI assistance (/finale)`.
  Both appear after the commit body, separated by a
  blank line. Include attribution in the proposed
  message shown to the user (Step 3c) — the user MAY
  edit or remove it.

## 6. Scaffold Sync and Verification

- [x] 6.1 Copy the updated `.opencode/commands/finale.md`
  to `internal/scaffold/assets/opencode/commands/finale.md`
  to maintain byte-identity required by
  `TestEmbeddedAssets_MatchSource`.
- [x] 6.2 Run `make test` to verify drift detection
  passes and no regressions are introduced.

## 7. Documentation and Compliance

- [x] 7.1 Add a CHANGELOG.md entry under `## Unreleased`
  documenting the `/finale` enhancements (structured
  PR body, AI attribution in PRs and commits, review-
  council findings, PR template detection, shell
  injection fix).
- [x] 7.2 Verify constitution alignment: confirm the
  implementation satisfies Principles I (self-describing
  artifacts), III (provenance metadata), and V (input
  validation before shell execution) as assessed in
  proposal.md.
- [x] 7.3 [P] File a documentation issue in
  `unbound-force/website` for the `/finale` command
  changes (per constitution Cross-Repo Documentation
  requirement).
<!-- scaffolded by uf vdev -->
<!-- spec-review: passed -->
<!-- code-review: passed -->
